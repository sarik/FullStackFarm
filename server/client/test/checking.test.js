var all = require('chai');
var ass = all.assert;


describe('just checking my app', function(a) {
    
it('just', () => {

    arr = "";
    ass.equal(arr.length, 0, "lets see");

   });


 
});
